name: bookworm-hole-api

services:
  api:
    build: .
    env_file:
      - ./.env
    ports:
      - "10000:8000"

  postgres:
    image: postgres:18-bookworm
    env_file:
      - ./.env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
